#!/bin/bash

outfile=/scratch_VESTEC_INSTALL/VESTEC/VESTEC-install/hwelt.out

echo "Hello, this is the DLR-HPDA cluster :)"
echo >> $outfile
date >> $outfile
echo "Hello, this is the DLR-HPDA cluster :)" >> $outfile
