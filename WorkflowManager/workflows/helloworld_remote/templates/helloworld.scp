#!/bin/bash

outfile=/home/knap_da/VESTEC/VESTEC-install/hwelt.out

echo "Hello, this is the DLR-HPDA cluster :)"
echo >> $outfile
date >> $outfile
echo "Hello, this is the DLR-HPDA cluster :)" >> $outfile
